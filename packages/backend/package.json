{
  "name": "@borderbrowser/backend",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 execute borderbrowser --local --file=./migrations/001_init.sql",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "hono": "^4.6.14",
    "stripe": "^17.5.0",
    "@neondatabase/serverless": "^0.10.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "wrangler": "^3.99.0"
  }
}
